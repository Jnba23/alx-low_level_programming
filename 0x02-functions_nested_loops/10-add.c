#include"main.h"
/**
 * add - entry point
 * add - adds two integers and return the results
 * @a: the first integer
 * @b: the second integer
 * Return: return the the result of adding the two integers 'a' and 'b'
*/
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
