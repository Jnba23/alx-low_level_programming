#include"main.h"
/**
 * _abs - compute the absolute value of an integer
 * @n: the number to be computed
 * Return: _abs returns an int
 */
int _abs(int n)
{

	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
